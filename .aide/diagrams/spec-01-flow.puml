@startuml spec-01-flow
!theme plain
title 子计划 1：前期设计 - 任务执行流程图

start

partition "1. 游戏概念定义" {
  :确定游戏名称;
  :定义核心循环 (Core Loop);
  :明确目标玩家;
  :确定游戏节奏;
}

partition "2. 世界观设定" {
  :构建仙侠世界观框架;
  :设计主角背景;
  :设计敌人来源;
  :确定简约几何+仙侠视觉融合方案;
}

partition "3. 核心玩法设计" {
  :设计操作方式;
  note right: 移动、攻击、闪避、技能
  :设计连击系统;
  :设计技能系统;
  :设计伤害计算;
}

partition "4. 角色系统设计" {
  :设计角色属性;
  note right: HP, ATK, DEF, SPD 等
  :设计成长机制;
  :设计技能池;
}

partition "5. 敌人系统设计" {
  :设计敌人分类;
  note right: 小怪、精英、Boss
  :设计行为模式;
  :设计难度曲线;
  :设计生成规则;
}

partition "6. Roguelike 系统设计" {
  :设计强化道具系统;
  :设计选择机制;
  note right: 每波结束后选择
  :设计稀有度分布;
  :设计 Build 构筑可能性;
}

partition "7. 无尽模式设计" {
  :设计波次递进机制;
  :设计难度曲线;
  :设计里程碑奖励;
  :设计结算评分系统;
}

partition "8. UI/UX 设计" {
  :设计主界面;
  :设计战斗 HUD;
  :设计技能选择界面;
  :设计暂停/结算界面;
}

:整理输出游戏设计文档 (GDD);

:用户审阅确认;

if (用户确认?) then (是)
  :完成子计划 1;
  stop
else (否)
  :根据反馈修改设计;
  note right: 返回对应环节调整
  stop
endif

@enduml
