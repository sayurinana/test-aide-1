@startuml cleanup-flow
skinparam defaultFontName "PingFang SC"
skinparam dpi 300
scale 0.3

title 清理"御剑无双"旧信息 - 执行流程

start

partition "步骤1: 清理项目根目录" {
  :重写 README.md 为通用模板;
  :删除 CHANGELOG.md;
  note right: task-now.md 保留
}

partition "步骤2: 删除 docs/ 目录" {
  :删除整个 docs/ 目录;
  note right
    包含 9 个 GDD 文档:
    - gdd-index.md
    - gdd-chapter1~8.md
  end note
}

partition "步骤3: 清理 .aide/ 目录" {
  :删除 .aide/project-docs/ 目录;
  :清理 .aide/branches.md 旧描述;
  note right: logs/ 保留历史记录
}

partition "步骤4: 验证" {
  :搜索"御剑无双"相关内容;
  if (找到残留内容?) then (是)
    :清理残留内容;
  else (否)
  endif
  :验证通过;
}

stop

@enduml
