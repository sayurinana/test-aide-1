@startuml cicd-task-flow
!theme plain
title 任务执行流程图：配置 GitHub Actions CI/CD

start

:修改 vite.config.js;
note right: 添加动态 base 路径支持

:创建 .github/workflows 目录;

:创建 deploy.yml;
note right
  - build 作业
  - deploy 作业
end note

:验证配置;
if (配置正确?) then (是)
  :更新 README.md;
  :更新 CHANGELOG.md;
  :任务完成;
else (否)
  :修复配置问题;
  note right: 回退修正
endif

stop
@enduml
